# Estratégia

Preencher após definir as collections: categorias (smoke, funcional, negativos, authz, CRUD, integração, regressão, carga) e o que fica de fora (produção, `service_role`, carga em `/api/gerar-ata`).
